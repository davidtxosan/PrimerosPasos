
public class Calculos_conMath {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		//double raiz=Math.sqrt(9);
		double a=5;
		//double b=2;
		
		/*para calcular la raiz cuadrada se puede crear una variable con el metodo Math.sqrt y darle un valor
		o dentro de la sentencia poner el metodo directamente*/
		
		//double raiz=Math.sqrt(a);
		System.out.println(Math.round(a));
		
		/*para poder usar dos variables que son double con una que es int hay que obligar a decir 
		que la salida la haga en int con (int)antes de la formula*/
		
		/*int c=(int)Math.pow(a, b);
		System.out.println(c);*/
		
		//double base=5;
		
		//double exponente=3;
		
		//int resultado=(int)Math.pow(base, exponente);
		
		//System.out.println("el resultado de elevar el " + base + " al exponente " + exponente + " da un resultado de "+ resultado);

	}

}
